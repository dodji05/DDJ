{% extends 'base.html.twig' %}

{% block title %}CategoriesArticles{% endblock %}

{% block body %}
    <h1>CategoriesArticles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categories_article.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categories_article.libelle }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categories_article.slug }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ categories_article.createdat ? categories_article.createdat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ categories_article.updatedAt ? categories_article.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_artilces_index') }}">back to list</a>

    <a href="{{ path('admin_artilces_edit', {'id': categories_article.id}) }}">edit</a>

    {{ include('BackEnd/Main/articles/_delete_form.html.twig') }}
{% endblock %}
